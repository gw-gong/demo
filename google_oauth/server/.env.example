# 复制这个文件到同目录，并重命名为 .env，然后修改里面的内容
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# 可选，用于 JWT 签名，不填则使用默认值（兼容旧版 SESSION_SECRET）
# JWT_SECRET=
# 可选，前端地址，用于 CORS 和登录后重定向，默认 http://127.0.0.1:5173
# FRONTEND_URL=http://127.0.0.1:5173